import React from 'react';

import { SyncLoader } from 'react-spinners';

const Spinner = () => {
    return (
        <div>
            <SyncLoader color="#4D2EDD" />
        </div>
    );
};

export default Spinner;